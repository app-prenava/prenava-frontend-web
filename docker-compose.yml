services:
  prenava-frontend:
    build: .
    container_name: prenava-frontend
    ports:
      - "3000:80"
    restart: unless-stopped
    # If using environment variables, uncomment and map them below
    # environment:
    #   - VITE_API_BASE_URL=http://your-backend-url/api
